import React from 'react';
import FrontLayout from "layout/FrontLayout";

const connection = () => {
    return (
        <FrontLayout>
            <div>
                hello connections
            </div>
        </FrontLayout>
    );
};

export default connection;